/**
 * Arena Grandmaster Logger (Legacy Redirect)
 * This file exists to prevent build failures during path transitions.
 */
import { logger as arenaLogger } from '../context/arena-init';

export const logger = arenaLogger;
